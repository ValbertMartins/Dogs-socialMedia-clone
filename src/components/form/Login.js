import React from 'react'
import styles from "../../css/Login.module.css"
const Login = () => {
  return (
    <section className={`container ${styles.loginSection}`}>Login</section>
  )
}

export default Login